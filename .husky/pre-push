#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "🔄 Running build before push..."

if pnpm run build; then
  echo "✅ Build successful! Proceeding with push..."
  exit 0
else
  echo "❌ Build failed! Push is blocked."
  exit 1
fi